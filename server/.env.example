# ============================================
# DEPRECATED - Use root .env instead
# ============================================
#
# The backend server now reads configuration from the root .env file
# (one level up from this directory). This allows sharing configuration
# between frontend and backend.
#
# See the root .env.example for all available settings.
#
# The backend supports both prefixed and non-prefixed variable names:
#   - VITE_NOTION_API_KEY or NOTION_API_KEY
#   - VITE_NOTION_DB_MISSION or NOTION_DB_MISSION
#   - etc.
#
# Backend-specific settings (no VITE_ prefix):
#   - PORT (default: 3001)
#   - CORS_ORIGIN (default: http://localhost:5173)
#   - CACHE_TTL_SECONDS (default: 300)
#   - CACHE_CHECK_PERIOD (default: 60)
