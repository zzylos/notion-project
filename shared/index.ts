/**
 * Shared module exports for both client and server.
 *
 * This module contains types and constants that are used by both
 * the frontend (src/) and backend (server/) code.
 *
 * @example
 * // In client code:
 * import { WorkItem, PROPERTY_ALIASES } from '../shared';
 *
 * // In server code:
 * import { WorkItem, PROPERTY_ALIASES } from '../../shared';
 */

export * from './types';
export * from './constants';
